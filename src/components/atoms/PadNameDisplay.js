import React from 'react';
import './PadNameDisplay.scss';

const PadNameDisplay = () => {
  return (
    <div id="pad-name-display">
      <span id="pad-key">Q</span>
      <span id="pad-name">Heater-1</span>
    </div>
  );
};

export default PadNameDisplay;